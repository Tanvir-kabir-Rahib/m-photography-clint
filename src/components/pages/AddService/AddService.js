import React from 'react';
import useTitle from '../../../useTitle/useTitle';


const AddService = () => {
    useTitle('Add Services')
    return (
        <div>
            <h1 className='text-center mt-48  text-5xl font-bold text-gray-400'>Still on Development</h1>
        </div>
    );
};

export default AddService;